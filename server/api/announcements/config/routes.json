{
  "routes": [
    {
      "method": "GET",
      "path": "/announcements",
      "handler": "announcements.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/announcements",
      "handler": "announcements.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/announcements",
      "handler": "announcements.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
